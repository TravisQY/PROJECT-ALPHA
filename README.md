---

# 🚀 Next-Gen Tech Coming Soon Page

一个极简主义、富有科技感的“网站建设中”单页面。采用全 CDN 依赖设计，无需任何构建流程，即传即用。

![License](https://img.shields.io/badge/license-MIT-blue.svg)
![Status](https://img.shields.io/badge/status-live-brightgreen.svg)

## ✨ 特性

- **极简科技美学**：基于深色调和霓虹发光元素的视觉设计。
- **动态粒子背景**：使用 `tsParticles` 实现的轻量化交互式粒子连线效果。
- **实时倒计时**：精确到秒的自动化倒计时功能，采用科技感字体显示。
- **极致轻量**：
  - 零本地资源：所有库均通过 **CDN** 加载（Tailwind CSS, Lucide Icons, Google Fonts）。
  - 单文件结构：全部代码集成在 `index.html` 中，方便维护和迁移。
- **完全响应式**：完美适配手机、平板及桌面显示器。

## 🛠️ 技术栈

- **样式**: [Tailwind CSS](https://tailwindcss.com/)
- **粒子效果**: [tsParticles](https://particles.js.org/)
- **图标**: [Lucide Icons](https://lucide.dev/)
- **字体**: [Google Fonts (Orbitron & Inter)](https://fonts.google.com/)

## 🚀 快速部署 (GitHub Pages)

1. **创建仓库**：在 GitHub 上创建一个新仓库（例如 `coming-soon`）。
2. **上传文件**：将 `index.html` 上传至仓库根目录。
3. **开启页面**：
   - 进入仓库的 **Settings** -> **Pages**。
   - 在 **Build and deployment** 下的 **Branch** 选项中选择 `main` 分支。
   - 点击 **Save**。
4. **访问**：几分钟后，你的网页将通过 `https://<你的用户名>.github.io/<仓库名>/` 在线预览。

## ⚙️ 自定义设置

### 修改倒计时目标日期
在 `index.html` 文件末尾的 `<script>` 标签中，找到以下代码：
```javascript
// 设置为从现在起14天后
const targetDate = new Date();
targetDate.setDate(targetDate.getDate() + 14); 

// 若要设置固定日期，请使用：
// const targetDate = new Date('2024-12-31T23:59:59').getTime();
```
将其修改为你期望的发布日期即可。

### 修改粒子颜色或数量
找到 `tsParticles.load` 配置部分：
- 修改 `particles.number.value` 来增减粒子密度。
- 修改 `particles.color.value` 来更改粒子颜色（例如改为 `#ffffff`）。

## 📄 开源协议

本项目基于 [MIT License](LICENSE) 协议开源。

---

**💡 提示**: 如果你觉得这个模板对你有帮助，请给一个 ⭐️ 以示支持！
